def bubble_sort(array):
    n = len(array)
    for i in range(0, n):
        for j in range(0, n - 1):
            if array[j] > array[j + 1]:
                array[j + 1], array[j] = array[j], array[j + 1]
        print(f"{i=}", array)

bubble_sort([3, 2, 4, 1])